const prompt = require('prompt');
prompt.start();


prompt.get('input', function (err, result) {
  
   
    if (a==b==c || a==b==d || a==c==d || b==c==d ){
        console.log("YES");
    }else {
        console.log("NO");
    }let number = result
    let b = number[0];
    let c = number[1];
    let d = number[2];
    let e = number[3];
});
